import React from 'react';

/* Components ---------------------------*/
import Layout from './Layout/Layout.jsx';
import Routing from './Routing.jsx';

const UI = () => {
    return (
        <Layout>
            <Routing />
        </Layout>
    );
}

export default UI;